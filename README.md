# 🚀 Startup Auto-Launcher System

Система автоматического запуска приложений из папки автозагрузки с интеллектуальной проверкой состояния и запуском от администратора.

## 📋 Возможности

- ✅ **Автоматический запуск** приложений при входе в Windows
- ✅ **Проверка уже запущенных** приложений (не дублирует запуск)
- ✅ **Запуск с правами администратора** при необходимости
- ✅ **Логирование** всех действий
- ✅ **Пропуск системных аккаунтов** (SYSTEM, SERVICE и т.д.)
- ✅ **Задержка запуска** (настраиваемая)

## 🛠️ Установка

### Способ 1: Быстрая установка (рекомендуется)

1. Скачайте или клонируйте репозиторий:
```powershell
git clone https://github.com/dictovod/setup_autostart.git
cd setup_autostart
```

2. Запустите установщик **от имени администратора**:
```powershell
powershell.exe -ExecutionPolicy Bypass -File "setup_autostart.ps1"
```

### Способ 2: Ручная установка

1. Создайте папку для скриптов:
```powershell
New-Item -ItemType Directory -Path "C:\Scripts" -Force
```

2. Скопируйте `startup_check.ps1` в `C:\Scripts\`

3. Создайте задачу в планировщике:
```powershell
schtasks /create /tn StartupAppsLauncher /tr 'powershell.exe -ExecutionPolicy Bypass -WindowStyle Hidden -File "C:\Scripts\startup_check.ps1"' /sc onlogon /delay 0000:30 /ru "%USERDOMAIN%\%USERNAME%" /rl highest /f
```

## 🎯 Использование

### Добавление приложений в автозагрузку

1. Создайте ярлыки для нужных приложений
2. Поместите их в папку автозагрузки:
   ```
   %APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup
   ```
   Или откройте ее через: `Win + R` → `shell:startup`

3. Система автоматически запустит их при следующем входе в систему

### Просмотр логов

Логи сохраняются в: `C:\Scripts\startup_user_log.txt`

Для просмотра последних записей:
```powershell
Get-Content "C:\Scripts\startup_user_log.txt" -Tail 20
```

## ⚙️ Настройка

### Изменение задержки запуска

Удалите текущую задачу и создайте с новой задержкой:

```powershell
# Удалить задачу
schtasks /delete /tn "StartupAppsLauncher" /f

# Создать с задержкой 10 секунд
schtasks /create /tn StartupAppsLauncher /tr 'powershell.exe -ExecutionPolicy Bypass -WindowStyle Hidden -File "C:\Scripts\startup_check.ps1"' /sc onlogon /delay 0000:10 /ru "%USERDOMAIN%\%USERNAME%" /rl highest /f
```

### Изменение пути к лог-файлу

Отредактируйте строку в `startup_check.ps1`:
```powershell
$logFile = "C:\Scripts\startup_user_log.txt"  # Измените на нужный путь
```

## 📁 Структура проекта

```
setup_autostart/
├── startup_check.ps1          # Основной скрипт проверки автозагрузки
├── setup_autostart.ps1        # Установщик системы
├── README.md                  # Документация
└── LICENSE                    # Лицензия (MIT)
```

## 🔧 Технические детали

### Как это работает

1. При входе пользователя в Windows запускается задача планировщика
2. Через заданную задержку (по умолчанию 30 сек) запускается скрипт
3. Скрипт проверяет папку автозагрузки текущего пользователя
4. Для каждого ярлыка (.lnk) проверяется, запущено ли целевое приложение
5. Если приложение не запущено - предпринимается попытка запуска:
   - Сначала с правами администратора
   - Если не удалось - обычным способом
6. Все действия логируются в файл

### Особенности

- **Игнорирование системных аккаунтов**: Не запускается для SYSTEM, SERVICE и т.д.
- **Безопасный запуск**: Обработка ошибок и исключений
- **Минимальная нагрузка**: Проверка только при входе в систему
- **Поддержка UAC**: Запрос прав администратора при необходимости

## 🐛 Устранение неполадок

### Задача не запускается
```powershell
# Проверьте состояние задачи
Get-ScheduledTask -TaskName "StartupAppsLauncher"

# Запустите вручную
Start-ScheduledTask -TaskName "StartupAppsLauncher"
```

### Приложения не запускаются
1. Проверьте, есть ли ярлыки в папке автозагрузки
2. Убедитесь, что пути в ярлыках корректны
3. Проверьте логи: `C:\Scripts\startup_user_log.txt`

### Ошибка прав доступа
Запускайте PowerShell от имени администратора при установке.

## 📝 Лицензия

MIT License - смотрите файл [LICENSE](LICENSE)

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📞 Поддержка

Если у вас возникли проблемы или вопросы:
- Создайте Issue в репозитории
- Проверьте логи в `C:\Scripts\startup_user_log.txt`
- Убедитесь, что PowerShell разрешает выполнение скриптов

---

⭐ Если этот проект был полезен, поставьте звезду на GitHub!
